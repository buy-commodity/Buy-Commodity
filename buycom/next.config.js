/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1', 'localhost', 'tech.buycommodity.in'], // Add both '127.0.0.1' and 'localhost'
    },
}

module.exports = nextConfig
